export interface Video { // Exporta la interfaz Video para ser usada en otros archivos
  id: string;        // Identificador único del video
  title: string;     // Título del video
  thumbnail: string; // URL de la imagen miniatura del video
  src: string;       // URL de la fuente (archivo) del video
} 